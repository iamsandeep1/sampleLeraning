Process of the program:
1> Taking the IP from data.json
2> Validating the ip based on RFC1918 and basic protocol, The address is reserved or not
3> if ip is valid, trying to ping it
4> Not valid ip would act as exception